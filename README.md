# status-page
Status page for widget monitoring db

Use:

```shell
$ cp env.sh.template env.sh
$ chmod u+x env.sh
-edit env.sh, choose AZ region

$ terraform init
$ terraform plan
$ terraform apply
```

